import type { ReactElement } from "react";

/**
 * ダッシュボード画面(screen-design/README.md §1)。
 * 現時点ではルーティング疎通確認用のプレースホルダで、後続フェーズで実装する。
 */
export const Dashboard = (): ReactElement => <h1 className="text-xl font-bold">ダッシュボード</h1>;
