import type { ReactElement } from "react";

/**
 * 点検校正項目一覧画面。
 * 現時点ではルーティング疎通確認用のプレースホルダで、後続フェーズで実装する。
 */
export const ItemList = (): ReactElement => <h1 className="text-xl font-bold">点検校正項目一覧</h1>;
