import type { ReactElement } from "react";

/**
 * 機器詳細画面。
 * 現時点ではルーティング疎通確認用のプレースホルダで、後続フェーズで実装する。
 */
export const EquipmentDetail = (): ReactElement => <h1 className="text-xl font-bold">機器詳細</h1>;
