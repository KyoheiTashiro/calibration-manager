import type { ReactElement } from "react";

/**
 * 機器登録・編集画面。
 * 現時点ではルーティング疎通確認用のプレースホルダで、後続フェーズで実装する。
 */
export const EquipmentForm = (): ReactElement => (
  <h1 className="text-xl font-bold">機器登録・編集</h1>
);
