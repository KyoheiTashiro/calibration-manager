# 1. ダッシュボード

[画面設計 共通仕様・全体構成](./README.md) の一部。ステータス色・モーダル共通挙動・確認ダイアログ等の共通仕様は [README(§0 共通仕様)](./README.md#0-共通仕様) を参照。

**目的**: 要対応の全体像を一目で把握し、対応が必要な項目へ最短で到達させる。

**URL**: `/`

## 画面レイアウト

```
┌──────────────────────────────────────────────────────────┐
│ ダッシュボード                                           │
│ ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐         │
│ │期限切れ  │ │ 要発注  │ │期限接近  │ │ 校正中  │        │
│ │   3     │ │   2     │ │   5     │ │   1     │  ← カード│
│ │ (赤)   │ │ (橙)   │ │ (黄)   │ │ (青)   │         │
│ └─────────┘ └─────────┘ └─────────┘ └─────────┘         │
│                                                          │
│ 要対応項目                                               │
│ ┌──────────────────────────────────────────────────────┐│
│ │ 状態 管理番号 機器名 項目名 種別 担当 次回期限        ││
│ │ 🔴  EQ-001  ノギス 年次校正 校正 田中 2026-06-20     ││
│ │ 🟠  EQ-014  はかり 校正   校正 佐藤 2026-08-01      ││
│ │ 🟡  EQ-007  マイク 月次点検 点検 鈴木 2026-07-25     ││
│ └──────────────────────────────────────────────────────┘│
│                                                          │
│ 最新の通知                              [通知センターへ] │
│ • [overdue] EQ-001 年次校正が期限超過        2026-07-01 │
│ • [dueSoon] EQ-007 月次点検が近づいています   2026-07-02 │
└──────────────────────────────────────────────────────────┘
```

## 表示項目

- **サマリーカード4枚**: `overdue` / `orderNow` / `dueSoon` / `inProgress` それぞれの該当項目件数。件数は全 `InspectionItem`(`isActive=true` かつ機器 `status=active`)に対し [§0.3](./README.md#03-ステータスバッジ色共通定義) の導出ステータスを集計。カード色は [§0.3](./README.md#03-ステータスバッジ色共通定義) に準拠。
- **要対応項目リスト**: 各行に InspectionItem の `管理番号`(Equipment.managementNo)、`機器名`(Equipment.name)、`項目名`(InspectionItem.name)、`種別`(type)、`担当者`(Person.name via personId)、`次回期限`(nextDueDate)、ステータスバッジ。
- **最新の通知5件**: Notification の `type` / `message` / `createdDate`(未読を優先、新しい順)。

## 操作・アクション

- サマリーカードクリック → `/inspection-items?status=<overdue|orderNow|dueSoon|inProgress>` へ遷移(該当ステータスでプリフィルタ)。
- 要対応項目リストの行クリック → 該当機器の詳細 `/equipment/:id`(または項目一覧の該当行)へ遷移。
- 「通知センターへ」→ `/notifications`。

## 表示ルール・バリデーション

- 要対応項目リストの並び順: **優先度 `overdue` → `orderNow` → `dueSoon` の順、同グループ内は nextDueDate 昇順**。`inProgress`・`ok` はリストに含めない(カード集計にのみ反映)。
- 集計対象は稼働機器(`status=active`)の有効項目(`isActive=true`)のみ。`suspended`/`retired` 機器の項目は除外(ドメインモデル §3.3)。
- 入力フォームなし(表示専用画面)。

## 空状態

- 要対応が0件: 「対応が必要な項目はありません 🎉」。
- 通知が0件: 「新しい通知はありません」。
