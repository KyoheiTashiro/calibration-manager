# 12. 利用マニュアル

[画面設計 共通仕様・全体構成](./README.md) の一部。ステータス色・モーダル共通挙動・確認ダイアログ等の共通仕様は [README(§0 共通仕様)](./README.md#0-共通仕様) を参照。

**目的**: アプリの目的・基本操作の流れ・ステータスの見方・期限計算式・各画面の説明・バックアップ方法をまとめた利用マニュアルを提供する。

**URL**: `/manual`

**非機能要件**: 静的コンテンツのみで構成し、store(Zustand)を一切参照しない。モーダルなし・入力なし・確認ダイアログなし。サイドバー最下部(設定の次)に配置する。

## 画面レイアウト

```
┌──────────────────────────────────────────────────────┐
│ 利用マニュアル                                        │
│ ── このアプリについて ────────────────────────────  │
│ (説明文 + 設定画面へのリンク)                         │
│                                                        │
│ ── 基本の流れ ────────────────────────────────────  │
│ 1. マスタ登録(メーカー・取引先/担当者)                │
│ 2. 機器登録                                           │
│ 3. 項目登録(機器詳細/点検校正項目一覧から起動)        │
│ 4. 実施記録登録(点検校正項目一覧、次回期限自動更新)   │
│ 5. 外部校正は案件で進捗管理                           │
│                                                        │
│ ── ステータスの見方 ──────────────────────────────  │
│ [期限切れ] 今日が次回期限を過ぎている                 │
│ [要発注]   外部校正で発注推奨日超過・進行中の案件なし │
│ [校正中]   外部校正で発注済み・校正中の案件がある     │
│ [期限接近] 次回期限が近づいている                     │
│ [正常]     上記のいずれにも該当しない                 │
│                                                        │
│ ── 期限と発注推奨日の計算 ────────────────────────  │
│ 次回期限 = 前回実施日 + 周期                          │
│ 発注推奨日(外部のみ) = 次回期限 − 納期 − 発注余裕日数 │
│                                                        │
│ ── 各画面の説明 ──────────────────────────────────  │
│ (8画面それぞれへのリンク + 目的の要約)                │
│                                                        │
│ ── バックアップと復元 ────────────────────────────  │
│ (設定画面へのリンク + CSVエクスポート/インポートの案内)│
└──────────────────────────────────────────────────────┘
```

## 表示項目 / 機能

- **このアプリについて**: アプリの目的(点検・校正期限の一元管理)、データが LocalStorage に保存されサーバへ送信されないこと、ブラウザ・端末ごとにデータが独立していること、定期バックアップの推奨と設定画面([§11](./11-settings.md))への案内。
- **基本の流れ**: 番号付きリスト(5ステップ)で、マスタ登録([§9](./09-masters.md))→機器登録([§3](./03-equipment-form.md))→項目登録([§6](./06-inspection-item-modal.md)、機器詳細/点検校正項目一覧から起動)→実施記録登録([§7](./07-record-modal.md)、点検校正項目一覧から起動)→外部校正案件管理([§8](./08-orders.md))の順に、各ステップから遷移可能な画面へのリンクを付す。
- **ステータスの見方**: [§0.3](./README.md#03-ステータスバッジ色共通定義) の5ステータスを定義順(優先度順: overdue→orderNow→inProgress→dueSoon→ok)にバッジ表示し、各ステータスの意味を平易な日本語で説明する。
- **期限と発注推奨日の計算**: ドメインモデル §4.1/§4.2 の計算式をそのまま記載する(次回期限 = 前回実施日 + 周期、月単位の加算は暦月ベース。発注推奨日(外部のみ) = 次回期限 − 納期 − 発注余裕日数、納期は項目個別設定がなければ依頼先の標準納期を使用)。
- **各画面の説明**: ダッシュボード([§1](./01-dashboard.md))・機器一覧/機器詳細([§2](./02-equipment-list.md)/[§4](./04-equipment-detail.md))・点検校正項目一覧([§5](./05-inspection-item-list.md))・校正案件([§8](./08-orders.md))・メーカー・取引先/担当者([§9](./09-masters.md))・通知([§10](./10-notifications.md))・設定([§11](./11-settings.md)、PWAとしてのインストール、CSVによるデータのエクスポート/インポート、データ全削除)の各画面へのリンクと、各画面の目的を2〜3文で要約する。
- **バックアップと復元**: 設定画面([§11](./11-settings.md))からのCSVエクスポート(UTF-8 BOM付き)/インポートの案内と、端末変更・ブラウザデータ消去に備えた定期エクスポートの推奨。

## 表示ルール・バリデーション(zod)

- 対象外(入力フォームを持たない静的コンテンツのため zod バリデーションはなし)。

## 空状態

- 対象外(store を参照しないため空状態は存在しない。常に同一の静的コンテンツを表示する)。
